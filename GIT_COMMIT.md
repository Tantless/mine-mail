# Git 提交信息规范

本文档规定 Mine Mail 的 Git 提交信息格式，供项目维护者和 AI Agent
在获得提交授权后使用。

## 基本格式

每条提交信息的首行必须使用以下格式：

```text
<提交标识>: <中文说明>
```

例如：

```text
feat: 支持写信时选择联系人
fix: 修复凭证失效后重复同步的问题
ui: 统一滚动条和提示框样式
release: 发布 v0.1.6
```

必须遵守以下规则：

- 提交标识使用本文档规定的小写英文标识。
- 标识后使用半角冒号 `:`，冒号后保留一个半角空格。
- 冒号后的提交说明必须构成完整的中文短语，不得使用英文句子、从句或动作短语。
- 无法合理翻译的专有名词、协议、平台、文件名、代码符号和版本号可以保留原文，
  但必须嵌入中文说明中，例如 Gmail、IMAP、SQLite、Windows、
  `local_version` 和 v0.1.6。
- 提交说明使用“动作 + 对象 + 必要场景或结果”的动宾结构。
- 首行末尾不加句号，首行总长度不超过 72 个字符。
- 不默认添加作用域，不使用 `feat(web): ...` 等格式；修改范围应直接写入
  中文说明。
- 一次提交只表达一个主要意图。不同意图的修改应拆分为不同提交。
- 历史记录中的英文提交不属于本规范的范例，不得复制英文任务标题、分支名称或
  旧提交说明作为新的提交信息。

以下提交符合规范：

```text
ui: 调整邮件列表头像比例
fix: 修复 Linux 下长邮件导致的界面错位
```

以下提交不符合规范：

```text
fix(ui): align mail-list avatar proportions
fix: render sent replies from outbox content
```

## 提交标识

| 标识 | 使用场景 | 示例 |
| --- | --- | --- |
| `feat` | 新增用户能力或产品功能 | `feat: 支持联系人备注和头像覆盖` |
| `fix` | 修复错误、异常或回归 | `fix: 修复 Linux 下长邮件导致的界面错位` |
| `ui` | 只调整视觉、布局、动效、交互细节或界面文案 | `ui: 优化夜色主题开关关闭态配色` |
| `perf` | 提升速度、内存占用或同步效率 | `perf: 缩短缓存邮件的首次回显时间` |
| `refactor` | 重构内部实现，不改变产品行为 | `refactor: 拆分邮件同步与状态更新流程` |
| `docs` | 修改说明、规范和项目文档 | `docs: 补充 OAuth 配置说明` |
| `test` | 新增或调整测试 | `test: 补充草稿版本冲突测试` |
| `ci` | 修改 GitHub Actions 等自动化流程 | `ci: 支持验证并发布现有草稿版本` |
| `build` | 修改构建、依赖、安装器或打包配置 | `build: 更新 Windows 安装器配置` |
| `chore` | 处理 `.gitignore`、文件清理等日常维护 | `chore: 忽略本地 OAuth 配置文件` |
| `release` | 正式发布版本 | `release: 发布 v0.1.6` |
| `revert` | 回滚已有提交 | `revert: 回滚邮件列表虚拟滚动改动` |

不要自行引入表外标识。确需新增标识时，应先取得用户同意并更新本文档。

## 标识选择

按照提交带来的主要结果选择标识：

1. 用户获得了此前没有的能力，使用 `feat`。
2. 原本应该正常工作的功能存在错误，使用 `fix`。
3. 产品能力不变，只调整界面呈现或交互细节，使用 `ui`。
4. 功能不变，但速度更快或资源占用更低，使用 `perf`。
5. 用户可感知行为不变，只整理内部实现，使用 `refactor`。
6. 仅修改文档、测试、持续集成、构建配置或维护文件时，使用对应的
   `docs`、`test`、`ci`、`build` 或 `chore`。

同一功能相关的实现、测试和规范文档可以放在一个提交中，并根据主要产品结果
选择 `feat` 或 `fix`。不要仅因提交包含测试或文档就改用 `test` 或 `docs`。

常见边界如下：

- 修复已经出现的界面错误使用 `fix`；主动调整视觉方案使用 `ui`。
- 优化运行性能使用 `perf`；只整理代码结构使用 `refactor`。
- 修改构建流程或依赖使用 `build`；修复最终用户已经遇到的安装或运行错误
  使用 `fix`。
- 版本发布本身使用 `release`；发布流程配置使用 `ci` 或 `build`。

## 中文说明写法

优先使用能够明确说明结果的动词，例如：

- 新增、支持、修复、避免
- 统一、优化、调整、重构
- 补充、更新、移除、发布、回滚

说明中应包含具体对象，必要时补充发生场景。不要使用无法判断修改内容的模糊
描述，例如：

```text
fix: 修改一些问题
feat: 功能完善
chore: 补充提交
docs: 更新内容
```

应改为可以独立理解的描述：

```text
fix: 修复通讯录首次进入时未初始化的问题
ui: 统一设置页按钮的强调色
chore: 忽略安装器生成目录
docs: 补充本地开发环境搭建步骤
```

## 提交正文

简单修改通常只需要首行。复杂修改可以在首行后空一行添加中文正文，说明修改
原因、关键约束或影响。正文同样以中文为主，专有名词和代码符号可以保留原文。

```text
fix: 修复凭证失效后的重复同步

凭证失效时仅停止对应账户的网络任务，并保留缓存邮件供离线阅读。
```

## AI Agent 提交检查

AI Agent 在获得用户提交授权后，应在创建提交前完成以下检查：

1. 查看工作区和暂存区差异，只提交当前任务范围内的修改。
2. 确认没有覆盖或混入用户及其他任务的未提交修改。
3. 根据主要结果从本文档的标识表中选择一个标识。
4. 使用准确、具体的中文说明，不照抄任务名称或使用模糊概括。
5. 确认提交信息符合 `<提交标识>: <中文说明>` 格式。
6. 运行与修改范围相符的验证，并确认暂存内容不包含凭证、令牌、邮箱内容、
   完整本地路径或其他隐私数据。
7. 提交后运行 `git log -1 --format=%s` 检查实际提交说明；若不符合中文格式，
   应在交付前立即修正。
