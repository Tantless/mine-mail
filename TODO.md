# Mine Mail 封版待办与 Agent 执行状态

本文件跟踪完整功能版本的实现状态、封版验证和后续 Agent 顺序。它不是产品或设计
规范；发生冲突时，以 [`AGENTS.md`](AGENTS.md)、[`DESIGN.md`](DESIGN.md)、
[`docs/PRODUCT.md`](docs/PRODUCT.md)、[`docs/MAIL_RENDERING.md`](docs/MAIL_RENDERING.md)
和 [`docs/RELEASE.md`](docs/RELEASE.md) 为准。

## 状态与优先级

- **P0 — 功能候选版阻断**：核心闭环或安全边界不完整时不得进入候选验收。
- **P1 — 发布前高优先级**：实现已完成，但仍需人工、原生平台或视觉证据。
- **P2 — 收尾优化**：不阻断核心闭环，完成后降低包体或维护风险。
- 状态：`[ ] 未开始`、`[-] 进行中`、`[x] 已完成`、`[!] 外部或人工阻塞`。

“隐藏未完成功能”不能代替实现。正式界面不得保留可点击占位、伪空文件夹、
演示数据回退或把摘要冒充完整正文。

## 当前封版结论

- [x] **功能候选版自动门禁**：Root、React、Tauri、生产构建和静态边界检查通过。
- [!] **人工视觉门禁**：四主题、真实 WebView、窗口缩放、键盘和辅助技术仍需
  用户指定浏览器/运行环境后验收。
- [!] **原生平台门禁**：附件另存的文件系统矩阵、真实邮箱和安装包行为仍需在
  对应系统与私有测试账户上验收。
- [!] **公开稳定版门禁**：签名、公证、OAuth 生产审核、法务/安全文件、SBOM、
  发布审批和真实设备测试仍以 [`docs/RELEASE.md`](docs/RELEASE.md) 为准。

完成 A1–A10 与自动化门禁，代表代码可进入功能候选验收；不等于已经满足公开稳定版
发布条件，也不授权连接真实邮箱、发送邮件、签名或发布。

## A1–A10 已批准契约

### A1 — 归档邮箱发现与创建

- **状态**：[x]
- 使用 SPECIAL-USE 和服务商能力发现 Archive。
- 只在契约允许时创建固定 Archive；不猜测任意服务商文件夹名称。

### A2 — 删除与永久删除

- **状态**：[x]
- 普通删除移动到 Trash。
- 只有 Trash 内提供永久删除，并使用产品内二次确认。
- 不使用可能清除无关邮件的全局 EXPUNGE。

### A3 — 离线邮箱操作

- **状态**：[x]
- 星标、已读/未读、归档、移入垃圾箱和永久删除采用账户隔离持久队列。
- 本地状态立即可见；重启和断网不丢失意图。

### A4 — UIDVALIDITY 与不确定结果

- **状态**：[x]
- 每次 UID 操作绑定邮箱与 UIDVALIDITY，不在新 epoch 复用旧 UID。
- 远端结果未知先对账，不盲目重复移动、删除或发送。
- SMTP `delivery_unknown` 只能由用户明确“确认已投递”或承担重复风险后“重试一次”；
  `outbox_id + expected_attempts` CAS 防止重复点击和迟到结果覆盖。

### A5 — Archive/Trash 同步范围

- **状态**：[x]
- 启动、计划、托盘和手动同步覆盖所有账户。
- Archive、Trash 使用缓存优先列表、历史分页和本地缓存搜索。

### A6 — 收件附件安全另存

- **状态**：[x]
- Rust 从完整缓存 RFC822 中定位精确 MIME 部件；React 不接触字节、原文或完整路径。
- 安全文件名、准确类型/大小、取消、失败和逐附件状态已接通。
- 同目录临时文件完整写入并同步后，才用原子 no-clobber 发布最终名称；碰撞自动加
  数字后缀，绝不覆盖现有文件。
- FAT/exFAT/SMB 等真实文件系统矩阵仍列在 P1-07。

### A7 — 发件附件与草稿版本

- **状态**：[x]
- 附件进入账户/草稿隔离的管理区，并绑定稳定草稿 ID、`local_version`、冲突副本
  和 Outbox 精确版本。
- MIME 发送只读取不可变管理文件并验证 SHA-256；schema v17 为旧 NULL 摘要记录
  提供账户隔离、一次性 CAS 安全回填。
- 普通生产读取不存在只校验长度的降级路径；等长替换、缺失、reparse、跨账户和
  并发不一致均安全失败。

### A8 — 完整转发

- **状态**：[x]
- 转发由 Rust 基于完整正文和可信身份信息准备，默认保留原附件。
- React 不用列表摘要或原始 HTML 拼接转发内容。
- Subject/From/To/Cc 在 RFC2047 解码后移除控制字符和双向文本伪装，并有长度上限。
- 明确不带附件的转发始终给出提醒。

### A9 — 历史分页与搜索

- **状态**：[x]
- 使用不透明稳定游标，默认 50 条、上限 100 条。
- 新邮件、移动和多账户切换不会依赖漂移 offset。
- 搜索明确限定为已同步到 SQLite 的本地缓存。

### A10 — Outlook 发布范围

- **状态**：[x]
- 正式连接入口隐藏 Outlook，不显示“即将支持”占位卡片。
- 旧 Outlook 配置只保留缓存可读和迁移说明；不允许传统密码网络连接。

## P0 — 功能候选版实现

### P0-01 完整正文转发

- **状态**：[x]
- Rust/Tauri `prepare_forward` 窄接口、加载/失败/HTML 隔离/附件行为和前端恢复状态
  已接通并覆盖测试。

### P0-02 邮箱操作闭环

- **状态**：[x]
- Archive、Trash、归档、移入垃圾箱、永久删除、已读/未读、星标、离线队列和
  `delivery_unknown` 人工决策形成闭环。
- 重复操作、远端消失、UIDVALIDITY 变化、网络中断和不确定结果具有幂等或停止策略。

### P0-03 收件附件

- **状态**：[x]
- 完整 MIME 部件索引、原生 Save As、准确元数据、无覆盖发布和失败恢复已实现。
- 路径穿越、保留名称、碰撞、部分写入和并发发布具有自动化测试。

### P0-04 发件附件与不可变发送版本

- **状态**：[x]
- 新建草稿稳定化、附件添加/移除、版本冲突、重启恢复、Outbox 绑定和 SMTP MIME
  已实现。
- Bcc 只存在于实际 RFC822/envelope 的正确位置，不进入搜索、联系人、回复、转发、
  通知或日志。

### P0-05 历史加载

- **状态**：[x]
- 收件箱、已发送、归档和垃圾箱具有稳定分页、加载中、失败、重试和已到底状态。
- 后台插入、移动/删除和账户切换不破坏选择或重复历史项。

### P0-06 正式界面无死控件

- **状态**：[x]
- 筛选、更多操作、附件、文件夹数据源均已接通或移除。
- 生产源码与构建产物扫描拒绝“尚未实现”“即将支持”等占位文案。
- 不使用浏览器原生 `alert`、`confirm`、`prompt`、校验气泡或原生 `<select>`。

### P0-07 不透明消息 ID 与窄 IPC

- **状态**：[x]
- schema v14 使用稳定全局 UUIDv4 `public_id`；React 不接收 rowid、provider mailbox
  或 UID。
- 分页、正文、星标、移动、附件、转发、联系人和通知均通过账户绑定窄接口。
- 两个账户 rowid 相同、迟到 Promise、通知打开和 malformed ID 均有回归测试。

## P1 — 发布前高优先级

### P1-01 收件人详情

- **状态**：[x]
- From、To、Cc 和有权显示的 Bcc 分组呈现。
- 备注名不隐藏真实地址；Sent/Outbox 不再把收件人伪装成发件人。
- 长地址、Escape、焦点返回和视口边界具有自动化覆盖。

### P1-02 文件夹筛选、同步文案与空状态

- **状态**：[x]
- 每个文件夹使用有意义的筛选和同步名称。
- 真实为空、搜索无结果、筛选无结果、加载失败、尚未同步和已到底互不混淆。

### P1-03 Outlook 范围一致

- **状态**：[x]
- 本版仅开放 163、Gmail 和自定义 IMAP/SMTP；旧 Outlook 缓存保持只读。

### P1-04 键盘、焦点和操作目标

- **状态**：[x]
- 列表语义、方向键/焦点、对话框圈定、Escape、焦点恢复、`aria-live`、目标尺寸、
  reduced-motion 与 1250/940/720 断点具有代码和自动化验证。
- 真实 WebView 与辅助技术验收合并到 P1-06。

### P1-05 演示数据与生产构建隔离

- **状态**：[x]
- demo adapter、demo fixtures 和正式空写信模型已经分离。
- 普通浏览器不静默伪造邮箱；生产构建扫描拒绝 demo 标识和占位文案。
- CI 在构建后执行生产 bundle verifier。

### P1-06 四主题、响应式与辅助技术人工验收

- **状态**：[!]
- **阻塞原因**：产品设计审查只能使用用户指定的浏览器；当前尚未选择浏览器/真实
  WebView 环境，因此不得以代码测试或截图冒充视觉验收。

待验收：

- [ ] Daylight、Night、Dusk、Forest。
- [ ] 1440×900、1250/940 px 重排、720 px 单栏、高 DPI 与缩放。
- [ ] 收件箱、已发送、草稿、发件队列、归档、垃圾箱、联系人、设置和写信。
- [ ] 默认、悬停、按下、焦点、禁用、加载、空、错误、长地址和长主题。
- [ ] 附件、转发、收件人详情、永久删除和投递未知确认。
- [ ] 键盘全流程、屏幕阅读器提示和减少动态效果。

临时截图只能保存在系统临时目录，不提交仓库。

### P1-07 附件原生文件系统矩阵与路径竞态

- **状态**：[!]
- 自动化实现使用同目录临时文件和平台 no-replace 发布；最终名称不会出现半成品，
  也不会覆盖同名文件。
- [ ] Windows：NTFS、FAT32、exFAT 和选定 SMB 组合。
- [ ] macOS：APFS、exFAT 和选定 SMB 组合。
- [ ] Linux：ext4、vfat/exFAT 和选定 CIFS/SMB 组合。
- [ ] 验证磁盘满、权限变化、介质拔出、断网、并发同名和发布前中断。
- [ ] 在公开发布威胁模型中明确接受或消除 path-based 同用户目录替换竞态；若要求
  抵抗该攻击，必须先改为目录句柄锚定并补对抗测试。

详细门禁见 [`docs/RELEASE.md`](docs/RELEASE.md)。

## P2 — 收尾优化

### P2-01 前端包体与遗留样式

- **状态**：[x]
- Settings、Contacts、RichText 等非首屏路径已合理拆包。
- 生产构建不存在大于 500 KiB 的 JS chunk，且没有把离线可用首屏变成空白占位。
- 已移除重复/遗留实现并增加 bundle boundary 测试。

## 自动化封版证据

本节只记录最终可重复检查，不替代真实邮箱、原生平台或视觉验收。

- [x] Root：`cargo fmt --all -- --check`
- [x] Root：`cargo test --locked --no-fail-fast`（214 项库测试、3 项 CLI 测试和
  1 项 compile-fail 文档测试）
- [x] Root：`cargo check --locked`
- [x] React：`npm test -- --run`（26 个测试文件、389 项测试）
- [x] React：`npm run build`（7 个 JavaScript chunk，最大 433.91 KiB）
- [x] React：`npm run verify:production-bundle`（入口 420.69 KiB，无 demo、fixture
  或占位文案）
- [x] Tauri：`cargo fmt --all -- --check`
- [x] Tauri：`cargo test --locked --no-fail-fast`（99 项测试）
- [x] Tauri：`cargo check --locked`
- [x] Repository：`git diff --check`
- [x] 静态扫描：无占位文案、旧 mailbox/UID IPC、前端 rowid/UID、原生浏览器
  对话框、有效 `dialog:allow-save` 权限或 hard-link-only 发布路径。

## Agent 执行记录与后续顺序

### 已完成波次

1. **产品契约 Agent**：确认 A1–A10，更新 PRODUCT、MAIL_RENDERING 和 DESIGN。
2. **邮箱核心 Agent**：邮箱角色、离线队列、UIDVALIDITY、对账和稳定分页。
3. **附件/MIME Agent**：收发附件、不可变草稿版本、转发、Bcc 和摘要迁移。
4. **Demo/构建 Agent**：演示隔离、懒加载、生产 bundle verifier 和 CI。
5. **Tauri 边界 Agent**：账户隔离 DTO、公开消息 ID、通知与原生文件流程。
6. **阅读器/写信 Agent**：附件、转发、收件人详情、确认流程和错误恢复。
7. **文件夹/列表 Agent**：Archive/Trash、分页、筛选、空状态和选择保持。
8. **可访问性 Agent**：键盘、焦点、ARIA、断点和 reduced-motion 自动化。
9. **安全收口 Agent**：`delivery_unknown`、转发身份净化、旧附件 SHA-256 回填和
   原子 no-clobber 发布。

### 下一批 Agent 必须按此顺序

1. **视觉验收 Agent**：用户先指定浏览器/真实 WebView，再执行 P1-06；只提交修复、
   canonical docs 和测试，不提交 QA 日志或截图。
2. **平台矩阵 Agent**：在隔离测试卷和共享目录执行 P1-07；不得使用用户真实附件或
   邮箱数据。
3. **真实邮箱集成 Agent**：使用明确提供的私有测试账户验证 163、Gmail、自定义服务、
   三账户、撤权、离线恢复和 UIDVALIDITY；任何发送或服务端变更需用户再次明确授权。
4. **发布工程 Agent**：只有用户确定发布渠道、平台、签名身份和审批人后，才处理
   LICENSE、SECURITY、签名/公证、OAuth 生产审核、SBOM 和受保护发布环境。

## 协作规则

- 每个 Agent 开工前读取 `AGENTS.md` 和对应 canonical docs。
- 同一未隔离工作树不得让多个 Agent 同时修改共享文件。
- 持久行为先更新产品契约；可见行为先遵守 DESIGN；MIME 改动先遵守 MAIL_RENDERING。
- 默认验证不得连接真实邮箱、改变服务器状态、发送邮件、发布、签名或创建 Release。
- 每个工作包必须回报修改、迁移、测试、残余风险和未运行的外部门禁。
- 未创建 commit 时不得虚构 commit subject；需要提交时再遵守 `GIT_COMMIT.md`。
