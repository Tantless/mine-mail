{
  "name": "mine-mail-review-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build && node --test tests/site.test.mjs",
    "test": "npm run check"
  },
  "devDependencies": {
    "vite": "6.4.2"
  }
}
